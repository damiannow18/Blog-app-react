import React from 'react';

function Info() {
    return (
    <div className='infoPage'>
        <div className='infoContainer'>This is a simple page that represents a blog. It is created with React.js. It uses both HTML and CSS. It has a connection with Firebase database which allows users to create, view and delete posts. The page implements authentication with Google</div>
    </div>);
}

export default Info;